# Database Configuration
MYSQL_ROOT_PASSWORD=change_me_in_production
MYSQL_DATABASE=openshort

# Backend Configuration
ASPNETCORE_ENVIRONMENT=Production

# JWT Secret Key (IMPORTANT: Change this in production!)
# Generate a secure random key with at least 32 characters
# Example (Linux/Mac): openssl rand -base64 32
# Example (Windows): [Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Maximum 256 }))
JWT_SECRET_KEY=CHANGE_ME_TO_A_SECURE_RANDOM_KEY_AT_LEAST_32_CHARS_LONG
